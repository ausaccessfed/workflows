name: 'Distribute Workflows'
on:
  workflow_dispatch:
  push:
    #  branches:
    #    - main
jobs:
  distribution:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3.6.0

      - uses: actions/github-script@d7906e4ad0b1822421a7e6a35d5ca353c962f410 # v6.4.1
        with:
          github-token: ${{secrets.ALL_REPO_SCOPED_TOKEN}}
          script: |
            const fs = require('fs')
            const script = require('./scripts/workflow-distributor.js')
            //  ${{ vars.RENOVATE_REPOSITORIES }}
            await script({github, context, repositories: ["ausaccessfed/reporting-service"], fs, glob})
